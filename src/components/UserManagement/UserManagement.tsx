
import React, { FC } from 'react';
import {Props} from './UserManagement.interface';
import useStyles from './UserManagement.module.scss';

const UserManagement: FC<Props> = () => {
  return (
    <div>
        This is UserManagement Component
    </div>
  );
};

export default UserManagement;
